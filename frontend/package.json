{
  "name": "@mediconnect/frontend",
  "version": "0.0.0",
  "private": true,
  "description": "MediConnect frontend (placeholder — scaffolded in P6).",
  "scripts": {
    "dev": "echo \"frontend not scaffolded yet — see .claude/sdd/frontend-mediconnect/tasks.md P6\" && exit 0",
    "build": "echo \"frontend not scaffolded yet\" && exit 0",
    "test": "echo \"frontend not scaffolded yet\" && exit 0"
  }
}
